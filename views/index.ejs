<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>bitly.js-a url shortner</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.5.3/dist/css/bootstrap.min.css" integrity="sha384-TX8t27EcRE3e/ihU7zmQxVncDAy5uIKz4rEkgIXeMed4M0jlfIDPvg6uqKI2xXr2" crossorigin="anonymous">

</head>
<body>
    <div class="container my-4">
        
    <h1 style="color: rgb(247, 99, 46);">bitly.<span class="text-secondary">js</span></h1>
    <form action="/shorturl" method="POST" class="my-4 form-inline">
        <label for="fullurl" class="sr-only">URL</label>
        <input type="text" name="fullurl" id="fullurl" placeholder="URl" required class="form-control mr-2 col-md-4 ">
        <button type="submit" class="btn bg-secondary text-white">Short</button>
    </form>
    <table class="table table-striped table-responsive">
        <thead>
            <tr>
                <th>Full URL</th>
                <th>Shorten URL</th>
                <th> Clicks</th>
            </tr>
        </thead>
        <tbody>
            <% shortUrls.forEach(shortUrl=>{ %>
                <tr>
                    <td>
                       <a href="<%= shortUrl.fullUrl %>"><%= shortUrl.fullUrl %></a> 
                    </td>
                    <td>
                       <a href="<%= shortUrl.shortUrl %>"><%= shortUrl.shortUrl %></a> 
                    </td>
                    <td>
                    <%= shortUrl.clicks %> 
                    </td>
                </tr>
            <% }) %>
            
        </tbody>
    </table>

    <p class="my-4 text-secondary">Web application developed by Jaydeep Shelake</p>

</div>
</body>
</html>

